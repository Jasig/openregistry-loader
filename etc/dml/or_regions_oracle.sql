-- Regions/States:

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'AK', 'Alaska', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'AL', 'Alabama', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'AR', 'Arkansas', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'AS', 'American Samoa', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'AZ', 'Arizona', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'CA', 'California', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'CO', 'Colorado', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'CT', 'Connecticut', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'DC', 'District of Columbia', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'DE', 'Delaware', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'FL', 'Florida', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'FM', 'Federated States of Micronesia', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'GA', 'Georgia', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'GU', 'Guam', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'HI', 'Hawaii', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'IA', 'Iowa', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'ID', 'Idaho', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'IL', 'Illinois', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'IN', 'Indiana', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'KS', 'Kansas', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'KY', 'Kentucky', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'LA', 'Louisiana', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'MA', 'Massachusetts', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'MD', 'Maryland', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'ME', 'Maine', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'MH', 'Marshall Islands', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'MI', 'Michigan', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'MN', 'Minnesota', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'MO', 'Missouri', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'MP', 'Northern Mariana Islands', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'MS', 'Mississippi', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'MT', 'Montana', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'NC', 'North Carolina', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'ND', 'North Dakota', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'NE', 'Nebraska', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'NH', 'New Hampshire', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'NJ', 'New Jersey', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'NM', 'New Mexico', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'NV', 'Nevada', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'NY', 'New York', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'OH', 'Ohio', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'OK', 'Oklahoma', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'OR', 'Oregon', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'PA', 'Pennsylvania', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'PR', 'Puerto Rico', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'PW', 'Palau', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'RI', 'Rhode Island', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'SC', 'South Carolina', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'SD', 'South Dakota', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'TN', 'Tennessee', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'TX', 'Texas', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'UT', 'Utah', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'VA', 'Virginia', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'VI', 'Virgin Islands', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'VT', 'Vermont', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'WA', 'Washington', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'WI', 'Wisconsin', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'WV', 'West Virginia', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'WY', 'Wyoming', c.id
   FROM ctd_countries c
    WHERE c.code = 'US');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'AB', 'Alberta', c.id
   FROM ctd_countries c
    WHERE c.code = 'CA');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'BC', 'British Columbia', c.id
   FROM ctd_countries c
    WHERE c.code = 'CA');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'MB', 'Manitoba', c.id
   FROM ctd_countries c
    WHERE c.code = 'CA');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'NB', 'New Brunswick', c.id
   FROM ctd_countries c
    WHERE c.code = 'CA');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'NF', 'Newfoundland', c.id
   FROM ctd_countries c
    WHERE c.code = 'CA');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'NL', 'Newfoundland and Labrador', c.id
   FROM ctd_countries c
    WHERE c.code = 'CA');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'NS', 'Nova Scotia', c.id
   FROM ctd_countries c
    WHERE c.code = 'CA');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'NT', 'Northwest Territories', c.id
   FROM ctd_countries c
    WHERE c.code = 'CA');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'NU', 'Nunavut', c.id
   FROM ctd_countries c
    WHERE c.code = 'CA');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'ON', 'Ontario', c.id
   FROM ctd_countries c
    WHERE c.code = 'CA');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'PE', 'Prince Edward Island', c.id
   FROM ctd_countries c
    WHERE c.code = 'CA');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'QC', 'Quebec', c.id
   FROM ctd_countries c
    WHERE c.code = 'CA');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'SK', 'Saskatchewan', c.id
   FROM ctd_countries c
    WHERE c.code = 'CA');

INSERT INTO ctd_regions (id, code, name, country_id)
 (SELECT ctd_regions_seq.nextval, 'YT', 'Yukon Territory', c.id
   FROM ctd_countries c
    WHERE c.code = 'CA');